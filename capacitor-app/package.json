{
  "name": "mqlc-admin-app",
  "version": "1.0.0",
  "description": "MQLC Admin Panel — Android WebView APK via Capacitor",
  "main": "index.js",
  "scripts": {
    "sync": "npx cap sync",
    "open": "npx cap open android",
    "build:android": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
